/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['localhost'], // Menggunakan 'domains' untuk konfigurasi localhost
  },
};

export default nextConfig;
